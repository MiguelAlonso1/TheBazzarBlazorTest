@inject NavigationManager NavigationManager
@rendermode InteractiveServer

<button @onclick="EditGame" class="btn btn-primary">Edit Stats</button>

@code {
    [Parameter]
    public int Id { get; set; }

    void EditGame()
    {
        NavigationManager.NavigateTo($"/edit-game/{Id}");
    }
}
